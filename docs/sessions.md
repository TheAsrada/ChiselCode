# Сессии и история

[Документация](README.md) · [Главная](../README.md)

Сессии хранятся отдельно от проекта в ChiselCode Home: `%LOCALAPPDATA%\ChiselCode\sessions` в Windows или `$XDG_DATA_HOME/chiselcode/sessions` (обычно `~/.local/share/chiselcode/sessions`) в macOS/Linux. `projects.json` связывает папку проекта с её ID; внутри `projects/<project-id>/` лежат `project.json`, быстрый `index.json` и отдельный JSON для каждого разговора. `/resume` и `/sessions` показывают разговоры только текущего проекта. В выборе можно искать по названию, последнему сообщению, ветке, модели, провайдеру или ID, посмотреть разговор клавишей Space, переименовать через Ctrl+R и удалить после подтверждения через Ctrl+D. `chisel --resume <id>` принимает полный ID или его однозначный префикс в текущем проекте.

Старые сессии копируются в новую структуру при открытии соответствующего проекта; исходные файлы остаются на месте. Индекс можно восстановить из файлов разговоров. `/clear` сохраняет текущий разговор, создаёт новый и оставляет на экране только шапку и поле ввода. Обычное обновление и удаление приложения сохраняют `sessions/` и пользовательские данные; для полного удаления данных их нужно удалить отдельно вручную.

## Повторный запуск из терминала

```bash
chisel --cwd ./my-project --resume <session-id>
chisel --cwd ./my-project --resume <session-id> "Продолжи разбор тестов"
```

Укажите ту же папку проекта, иначе нужный разговор не появится. `/clear` не удаляет сохранённую историю; удаление выполняется в выборе сессии и требует подтверждения.

## Резервная копия и приватность

Перед переносом или ручными изменениями остановите ChiselCode и скопируйте каталог `sessions/` целиком, включая индекс проектов. Сессии содержат сообщения и могут содержать код, результаты инструментов и полные diff. Они сохраняются как JSON, а не как зашифрованный архив: не добавляйте их в публичный репозиторий.

Расположение данных — в [конфигурации](configuration.md); технические детали diff — в [file-edit-ux.md](file-edit-ux.md).
